# Requirements for cell_HST_middle (cell-level HeteroST / HGT)
#
# These versions correspond to the local "baseline" development environment.
# CUDA builds are machine-specific; if the exact +cu130 wheel is unavailable,
# install the CPU/CUDA variant matching your driver from pytorch.org.

--index-url https://download.pytorch.org/whl/cu130

# Core deep-learning stack
torch==2.11.0+cu130

# Graph neural networks
torch-geometric==2.8.0

# Single-cell / spatial data
scanpy==1.12.1
anndata==0.12.14
h5py==3.12.1

# Scientific Python
numpy==2.1.3
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.6.1

# Config parsing
pyyaml==6.0.2
